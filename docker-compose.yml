version: '3'
services:
  influxdb:
    image: influxdb:1.4.2
    ports:
      - "8086:8086"
    environment:
      - INFLUXDB_DB=js_performance
  grafana:
    build: ./grafana
    ports:
      - "3000:3000"
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=true
